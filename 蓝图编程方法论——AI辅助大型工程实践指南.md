# 蓝图编程方法论——AI 辅助大型工程实践指南

> **Blueprint Coding: A Methodology for AI-Assisted Large-Scale Engineering**

**版本**：1.1  
**最后更新**：2026-05-24  
**定位**：独立方法论手册，可脱离任何具体项目携带与复用

---

## 目录

- [第一章 核心理念——为什么需要蓝图编程](#第一章-核心理念为什么需要蓝图编程)
  - [1.6 从 Vibe Coding 到蓝图编程](#16-从-vibe-coding-到蓝图编程agentic-engineering)
- [第二章 三层约束架构](#第二章-三层约束架构)
- [第三章 会话生命周期](#第三章-会话生命周期)
  - [3.2.4 上下文工程与 Token 预算](#324-上下文工程与-token-预算)
  - [3.2.5 Rules / Skills / Commands 分工](#325-rules--skills--commands-分工)
  - [3.2.6 渐进式检索与文件加载纪律](#326-渐进式检索与文件加载纪律)
- [第四章 域治理——边界、契约、归属](#第四章-域治理边界契约归属)
- [第五章 Phase 门控——防止范围蔓延](#第五章-phase-门控防止范围蔓延)
- [第六章 质量保障——从手动到系统化](#第六章-质量保障从手动到系统化)
- [第七章 反模式与失败图谱](#第七章-反模式与失败图谱)
- [第八章 多 Agent 协作](#第八章-多-agent-协作)
- [第九章 落地模板包](#第九章-落地模板包)
- [附录 C · 开源 Skills / Rules 参考](#附录-c--开源-skills--rules-参考)
- [附录 术语表](#附录-术语表)

---

## 第一章 核心理念——为什么需要蓝图编程

### 1.1 AI 编程的根本挑战

　　大语言模型（LLM）在代码生成领域展现了惊人能力——给定足够的上下文，它能在几秒内产出数百行结构完整的代码。然而，当项目从"一次性脚本"成长为"多域、多前端、多阶段的工程系统"时，裸用 AI 编程会反复撞上四堵墙：


| 挑战          | 表现                                      | 根因                    |
| ----------- | --------------------------------------- | --------------------- |
| **上下文窗口有限** | 会话后期 AI "忘掉"早期约定，产出与前半段矛盾的代码            | Token 上限固定，注意力权重衰减    |
| **无持久记忆**   | 新会话对上一轮的架构决策一无所知                        | 每次对话从零开始，无跨会话状态       |
| **意图偏离**    | AI 擅自引入新依赖、新表、新模式，偏离项目既定路线              | 缺乏约束时，AI 倾向于"过度建设"    |
| **一致性脆弱**   | 同一概念在后端叫 `match`、前端叫 `game`、文档叫 `event` | 无单一真相源，AI 每次从自身知识推断命名 |


　　这四堵墙的共同本质是：**AI 是无状态的强力执行者，但不是有记忆的架构守护者。** 它能精准地完成你描述的任务，但无法自行维护跨越时间和会话的工程一致性。

### 1.2 蓝图编程的定义

> **蓝图编程（Blueprint Coding）**：以声明式文档体系为约束层，引导 AI 在人类预定义的架构边界内高效产出代码的工程范式。

　　这个定义包含三个关键要素：

1. **声明式文档**——不是代码注释，不是口头叮嘱，而是结构化的、可被 AI 读取的 Markdown/YAML 文档，明确声明"什么能做、什么不能做、边界在哪里"。
2. **约束层**——文档不是被动的参考资料，而是主动的约束机制。AI 编程工具（如 Cursor）能在每次会话中自动注入这些规则，使其成为 AI 行为的"护栏"。
3. **架构边界内**——人类定义宏观结构（域、阶段、契约），AI 在结构内填充实现，将 AI 的能力导向正确的方向。

### 1.3 建筑蓝图隐喻

　　"蓝图编程"的名称借鉴了建筑行业的核心范式：


| 建筑工程                    | 蓝图编程                   |
| ----------------------- | ---------------------- |
| **建筑师**绘制蓝图，定义结构、材料、约束  | **人类**编写文档体系，定义域、阶段、契约 |
| **施工队**按蓝图施工，不自行改结构     | **AI**按文档约束编码，不自行改架构   |
| 蓝图是**权威真相源**，现场争议回蓝图    | 文档是**权威真相源**，编码争议回文档   |
| 蓝图**分层**：总图 → 结构图 → 水电图 | 文档**分层**：战略 → 战术 → 会话级 |
| 施工前**图纸会审**             | 编码前**会话简报 + 自检**       |
| 竣工后**竣工图与蓝图对齐**         | 推送前**代码与文档对齐**         |


　　这揭示了蓝图编程的核心哲学：**人类的价值不在于写每一行代码，而在于设计约束体系，让 AI 的强大产出力被引导向正确方向。**

### 1.4 与传统开发范式的对比


| 维度     | 传统开发                | 裸用 AI        | 蓝图编程                 |
| ------ | ------------------- | ------------ | -------------------- |
| 代码产出者  | 人                   | AI（无约束）      | AI（文档约束下）            |
| 架构一致性  | 靠团队共识 + Code Review | 靠运气          | 靠声明式文档 + 自动注入        |
| 新成员上手  | 读代码 + 问人            | 不适用          | 读文档体系即可开始指导 AI       |
| 跨会话连续性 | 人脑记忆 + Git 历史       | 无            | 文档 + 决策日志 + Phase 状态 |
| 范围控制   | 项目经理 + Sprint       | 无（AI 倾向过度建设） | Phase 门控 + 域边界       |
| 文档地位   | 代码的附属品（常过时）         | 无文档          | **文档是一等公民，先于代码**     |


### 1.5 蓝图编程的四个原则

　　所有后续章节的展开都基于以下四个核心原则：

- **原则一：文档先于代码**（Document-First）——先写约束，再让 AI 写实现。文档不是代码的注释，代码是文档的产物。
- **原则二：约束即自由**（Constraints as Freedom）——清晰的边界让 AI 无需猜测，反而产出更快更准。约束越明确，AI 越高效。
- **原则三：人管架构，AI 管实现**（Human Architects, AI Implements）——人类负责"做什么"和"不做什么"，AI 负责"怎么做"。
- **原则四：声明式优于命令式**（Declarative over Imperative）——用表格、清单、YAML 声明规则，而非在每次对话中反复口述。

### 1.6 从 Vibe Coding 到蓝图编程（Agentic Engineering）

　　2025 年初，Andrej Karpathy 提出 **Vibe Coding**：用自然语言描述意图，让 Agent（如 Cursor）生成代码，通过「运行 → 报错 → 粘贴错误 → 再生成」快速迭代，甚至「Accept All、不读 diff」——适合周末原型与 throwaway 项目。

　　到 2025～2026 年，专业团队的主流已从「纯 vibe」演进为 **Agentic Engineering**（智能体工程）：**保留 Agent 的杠杆，但不牺牲可维护性与质量**——规格先行、小步实现、可验证、人类保留架构判断力（Karpathy 所称的 *taste*）。

**三种范式对照：**

| 维度 | Vibe Coding（原型态） | Agentic Engineering（专业态） | 蓝图编程（本手册） |
|------|---------------------|------------------------------|-------------------|
| 意图表达 | 口语化、一次性 prompt | 规格 + 计划 + 检查点 | **L1 文档 + 会话简报** |
| 对代码的态度 | 可「忘记代码存在」 | 审查关键路径与契约 | **文档为真相源，代码须对齐** |
| 范围控制 | 无，易膨胀 | 任务分解 + 测试门 | **Phase 门控 + 域边界** |
| 跨会话记忆 | 聊天窗口 | 仓库内 spec / ADR | **00～09 + ADR + Skills** |
| 适用场景 | 个人实验、Demo | 产品级功能交付 | **多域、多阶段、长期仓库** |

**蓝图编程与 Vibe 的关系（不是对立，是分层）：**

```
┌─────────────────────────────────────────────────────────┐
│  L1/L2 蓝图约束（域、Phase、路由、表归属）  ← 人类架构师  │
├─────────────────────────────────────────────────────────┤
│  L3 会话内仍可「vibe」：小步描述、跑测、贴错误修  ← Agent  │
└─────────────────────────────────────────────────────────┘
```

　　在 **已划定边界** 的会话里，鼓励高效的 vibe 循环（描述子任务 → 生成 → 冒烟 → 修错）；在 **边界未定** 时，必须先回到 L1/L2 写清或更新文档，禁止让 Agent 自行「猜架构」。

**与 Spec-Driven / Memory-Bank 类实践的对齐：**

| 外部实践 | 蓝图编程中的落点 |
|----------|------------------|
| Game Design Document + Implementation Plan（[EnzeD/vibe-coding](https://github.com/EnzeD/vibe-coding/)） | L1 愿景/02- 赛制 + L2 域表；竖切计划写入 `04-` / 会话简报 |
| Constitution / SDD（规格驱动开发） | `00～09` 权威文档 + `docs/decisions/` ADR |
| Memory Bank（`architecture.md` / `progress.md`） | `08-` 工程详表 §三对齐度 + §5.1 变更记录 |
| Context Engineering（[muratcankoylan/Agent-Skills-for-Context-Engineering](https://github.com/muratcankoylan/Agent-Skills-for-Context-Engineering)） | 本章 §3.2.4～3.2.6：高信号、低 token、渐进加载 |

> **结论**：Vibe Coding 是 **执行层的交互风格**；蓝图编程是 **约束层的工程方法**。二者结合 = 「在蓝图划定的工地上 vibe 施工」。

---

## 第二章 三层约束架构

### 2.1 架构全景

　　蓝图编程的约束体系分为三层，从宏观到微观，从长期到短期：

```
┌─────────────────────────────────────────────────────┐
│  L1  战略蓝图（Strategic Blueprint）                  │
│  产品愿景 · 技术架构 · 阶段路线图                       │
│  变更频率：月级  │  维护者：产品负责人 / 架构师           │
├─────────────────────────────────────────────────────┤
│  L2  战术契约（Tactical Contract）                     │
│  域边界 · API 契约 · 表归属 · Phase 门控                │
│  变更频率：周级  │  维护者：技术负责人                    │
├─────────────────────────────────────────────────────┤
│  L3  会话控制（Session Control）                       │
│  开场简报 · 上下文注入 · 自检清单 · 推送对齐             │
│  变更频率：每次会话  │  维护者：当次 AI 编程操作者         │
└─────────────────────────────────────────────────────┘
```

　　三层之间是**自上而下的约束传递**关系：L1 定义方向，L2 将方向转化为可执行的边界规则，L3 确保每次 AI 会话遵守 L2 的规则。

### 2.2 L1 战略蓝图

　　战略蓝图回答的是"我们在建什么、为什么建、分几步建"。它是整个约束体系的根基，变更最不频繁，但影响最深远。

**组成要素：**


| 文档类型      | 内容               | 示例                              |
| --------- | ---------------- | ------------------------------- |
| **产品愿景**  | 平台定位、目标用户、核心价值主张 | "面向商科教育的模拟竞赛平台"                 |
| **技术架构**  | 技术栈选型、系统分层、部署架构  | "FastAPI + React + SQLite → PG" |
| **阶段路线图** | 分阶段交付计划、里程碑、优先级  | "Phase A 引擎闭环 → Phase B 智能体"    |
| **架构原则**  | 不可违反的技术决策        | "单库单进程""调度器单写者"                 |


**设计要点：**

- 战略蓝图应**稳定但不僵化**。月级更新频率意味着日常 AI 编程不应触及这一层。
- 每份文档应有明确的"最后更新"日期和"文档定位"首行，帮助 AI 快速判断其权威性和范围。
- 战略蓝图不需要包含实现细节，但必须包含**不可违反的约束**（如"禁止多数据库""禁止在 Phase A 做 Phase C 的事"）。

### 2.3 L2 战术契约

　　战术契约将战略蓝图的宏观方向转化为 AI 可直接遵守的具体规则。这一层是蓝图编程的**核心执行层**。

**组成要素：**


| 契约类型           | 作用                            | 形式               |
| -------------- | ----------------------------- | ---------------- |
| **域边界表**       | 声明每个域拥有哪些表、代码路径、禁止事项          | Markdown 表格      |
| **路由归属矩阵**     | 声明每类 API 归入哪个路由模块             | Markdown 表格      |
| **表归属声明**      | 每张数据表的唯一主人域                   | 域边界表的一列          |
| **Phase 门控表**  | 每个阶段可做/不可做的事项清单               | 二分 Markdown 表格   |
| **编码风格约定**     | 命名、类型注解、错误处理等统一规范             | 规则列表             |
| **API 契约（可选）** | OpenAPI/GraphQL Schema 作为域间接口 | YAML/JSON Schema |


**设计要点：**

- 战术契约的核心特征是**声明式**——用表格和清单，而非叙述性段落。表格对 AI 的可读性远优于散文。
- 理想情况下，战术契约应可被 AI 编程工具自动注入（如 Cursor 的 `alwaysApply` 规则），无需人工在每次会话中手动粘贴。
- 当代码变更导致契约过时时，必须**同步更新契约**。契约与代码的一致性是蓝图编程的生命线。

### 2.4 L3 会话控制

　　会话控制是面对每次具体 AI 编程对话的操作协议。它确保 L2 契约在每次会话中被正确执行。

**组成要素：**


| 控制机制        | 时机    | 作用                        |
| ----------- | ----- | ------------------------- |
| **会话简报**    | 会话开头  | 声明本次任务范围、Phase、涉及域、禁止项    |
| **上下文注入清单** | 会话开头  | 根据任务类型选择性 attach 对应文档     |
| **AI 自检协议** | 会话开头  | AI 主动确认 Phase、域、路由、表、架构模式 |
| **中间检查点**   | 执行过程中 | 每完成子任务，自检是否越界             |
| **推送前对齐门**  | 会话结束  | 代码与文档同步后才允许推送             |
| **Skills 工作流** | 按需触发 | 多步剧本：对齐、ADR、赛制扩展、排错（§3.2.5） |


**设计要点：**

- 会话控制的**自动化程度**决定了蓝图编程的实际效果。完全依赖人工执行的会话控制会被遗忘；能自动注入的规则（如 `.cursor/rules/`）效果最好；**长流程用 Skills**，避免 Rules 膨胀占满 token。
- 会话简报的价值在于**主动导航**，而非被动兜底。即使 AI 工具已自动注入规则，一句明确的开场声明仍能大幅减少 AI 的猜测和偏离。

### 2.5 三层映射示例

　　以一个教育模拟竞赛平台为例，展示三层如何具体落地：


| 层级  | 文档编号                                       | 文档名称       | 内容摘要              |
| --- | ------------------------------------------ | ---------- | ----------------- |
| L1  | 01                                         | 平台愿景与产品架构  | 五大域定义、发展主线        |
| L1  | 04                                         | 实施路线与里程碑   | Phase A→E 阶梯、成本估算 |
| L2  | 03                                         | 技术架构与实现现状  | 技术栈、域分包、智能体架构     |
| L2  | 08                                         | 工程现状与实现详表  | API/模型/百分比唯一真相源   |
| L2  | `.cursor/rules/blueprint-coding.mdc`       | 蓝图编程规范     | 域边界、路由归属、Phase 门控 |
| L3  | 09                                         | 分项目开发与集成流程 | 上下文注入清单、会话简报模板    |
| L3  | `.cursor/rules/docs-align-before-push.mdc` | 推送前文档对齐    | 推送前强制步骤           |
| L3  | `.cursor/skills/`（可选）                    | 按需工作流      | 推送对齐、排错、PR 审查等     |
| L3  | README                                     | 项目入口       | AI 编程会话开场指南       |


　　注意：编号体系（00-09）不是必须的，但**有编号的结构化文档体系**对 AI 的可读性和引用便利性远优于散落的 README。

---

## 第三章 会话生命周期

### 3.1 概览

　　每次 AI 编程会话都应遵循**三阶段生命周期**：准备（Pre）、执行（In）、收尾（Post）。大多数 AI 编程事故发生在跳过准备或收尾阶段时。

```
Pre-Session          In-Session              Post-Session
┌──────────┐    ┌───────────────────┐    ┌──────────────┐
│ 会话简报   │    │ 契约→模型→后端→前端 │    │ 文档对齐      │
│ 上下文注入 │ ─→ │ 中间检查点        │ ─→ │ 决策日志      │
│ AI 自检   │    │ 任务分解          │    │ 偏差回顾      │
└──────────┘    └───────────────────┘    └──────────────┘
```

### 3.2 Pre-Session：会话准备

#### 3.2.1 会话简报

　　会话简报是开场时人类主动告诉 AI 的"任务边界声明"。其价值在于：规则文件是被动的兜底护栏，而会话简报是主动的意图导航。二者缺一不可。

**简报应包含的要素：**


| 要素             | 说明         | 示例                            |
| -------------- | ---------- | ----------------------------- |
| **做什么**        | 一句话描述任务    | "新增投资人对决赛制"                   |
| **属于哪个 Phase** | 防止范围蔓延     | "Phase A"                     |
| **涉及哪些域**      | 划定代码触及范围   | "arena + games/investor_duel" |
| **禁止项**        | 明确"不要做的事"  | "不改 career 表、不动 trading 引擎"   |
| **完成定义**       | 什么状态算"做完了" | "组织者能创建赛事、学生能加入并完赛"           |


**简报模板：**

```markdown
## 本次边界
- 任务：[一句话描述]
- Phase：[A/B/C/D/E]
- 涉及域：[列出域名]
- 禁止：[明确列出不可触及的域/表/文件]

## 完成定义
- [ ] [可验证的条件 1]
- [ ] [可验证的条件 2]
- [ ] [可验证的条件 3]
```

#### 3.2.2 上下文注入策略

　　AI 的上下文窗口是有限资源，必须精打细算。核心原则是：**按任务类型选择性注入，而非一股脑全塞。**

**注入策略矩阵（通用版）：**


| 任务类型      | 必须注入                 | 可选注入     | 禁止注入     |
| --------- | -------------------- | -------- | -------- |
| 新增域/赛制    | 架构文档 + 域边界规则 + 扩展规范  | 相似域的实现参考 | 无关域的实现细节 |
| 后端 API 开发 | 路由归属表 + 数据模型文档       | 前端类型定义   | 前端 UI 代码 |
| 前端页面      | 功能矩阵 + API 列表 + 类型定义 | UI 设计稿   | 后端引擎实现   |
| Bug 修复    | 相关模块代码 + 错误日志        | 架构文档     | 全量文档     |
| 纯规划       | 愿景 + 路线图 + 技术架构      | 工程详表     | 具体代码文件   |


**经验法则**：单次会话注入的文档不宜超过**4-5 份**（或约 15,000 token 的文档内容）。超出后 AI 对末尾文档的注意力显著下降。

#### 3.2.3 AI 自检协议

　　在 AI 开始编码前，应回答以下五个自检问题。如果任何一项有疑问，应先向用户确认，而非假设后动手。


| #   | 自检问题                     | 关联约束     |
| --- | ------------------------ | -------- |
| 1   | 本次任务属于哪个 Phase？是否超出当前阶段？ | Phase 门控 |
| 2   | 涉及哪些域？是否存在跨域写入风险？        | 域边界      |
| 3   | 是否需要新增路由？归入哪个 API 模块？    | 路由归属     |
| 4   | 是否需要新建表？唯一主人域是谁？         | 表归属      |
| 5   | 是否涉及实时/并发？须遵守哪种并发模式？     | 架构约束     |


　　自检协议的理想实现方式是将其写入 AI 工具的自动注入规则中（如 Cursor 的 `alwaysApply` 规则），使 AI 在每次会话开始时自动执行。

#### 3.2.4 上下文工程与 Token 预算

　　有效上下文 ≠「把仓库相关文件全塞进去」。业界共识（见 [Agent Skills for Context Engineering](https://github.com/muratcankoylan/Agent-Skills-for-Context-Engineering)）：上下文窗口受 **注意力衰减** 制约——过长会出现 *lost-in-the-middle*、首尾偏重、后期约定被稀释。

**蓝图编程的 Token 预算原则：**

| 层级 | 预算建议 | 内容 |
|------|----------|------|
| **常驻（每请求）** | ≤ 2～4k tokens | 精简 `.mdc` 规则：域边界、Phase、自检 5 问、Shell/语言 |
| **会话注入（Pre）** | ≤ 12～15k tokens | 3.2.2 矩阵中的 4～5 份权威文档（摘章节，非全文） |
| **按需检索（In）** | 按文件递增 | 先 `grep`/语义搜索定位，再 **局部 Read**，禁止整库扫读 |
| **代码上下文** | 子任务相关 | 只打开将改动的域内文件；相似实现作 **单文件** 参考 |

**高信号 / 低噪声写法（写入 L1/L2 文档时）：**

- 表格与「禁止」列优于长段落叙述
- 文首 `> 文档定位` + `最后更新` 便于 Agent 判断是否过期
- 用 `08- §2.7` 这类 **锚点引用** 代替复制整段 API 列表
- 长文放 `inspire/`，根目录 `00～09` 保持可一次读完的密度

**压缩与续聊：**

| 场景 | 做法 |
|------|------|
| 会话将满 | 开新会话；简报 + **变更文件列表** + 未完成 checklist |
| 跨会话 | ADR、`08-` §5.1、`.cursor/breadcrumbs.md`（可选）记录「试过什么」 |
| 规划类任务 | Plan 模式只读，避免过早写代码占满上下文 |

#### 3.2.5 Rules / Skills / Commands 分工

　　Cursor 生态中，约束与流程应 **分层挂载**，避免把所有内容塞进一条 `alwaysApply`（既费 token 又难维护）。下表为蓝图编程推荐分工（与 [Cursor 官方文档](https://docs.cursor.com) 及社区实践一致）：

| 机制 | 路径示例 | 加载方式 | 适合承载 | 篇幅 |
|------|----------|----------|----------|------|
| **Rules** | `.cursor/rules/*.mdc` | 自动（可按 glob） | 域边界、Phase、命名、禁止项 | **单条 ≤50 行**，多文件拆分 |
| **Skills** | `.cursor/skills/<name>/SKILL.md` | Agent **按需** 或 `/skill` | 多步流程：推送前对齐、ADR、赛制扩展、排错仪式 | 可长，用时才读 |
| **Commands** | 斜杠命令 / 短 prompt 模板 | 用户显式触发 | 省 token 的固定动作：`/type-check`、`/lint` | 极短 |
| **User Rules** | Cursor 设置 | 全局 | 语言、OS/Shell、回复风格 | 极简 |
| **AGENTS.md** | 仓库根 | 部分工具读取 | 子目录 Agent 说明（Monorepo） | 按包 |

**与三层约束的映射：**

```
L1/L2 硬约束  →  Rules（blueprint-coding.mdc、docs-align.mdc、adr-writing.mdc）
L3 流程剧本   →  Skills（docs-align-before-push、create-skill、ci-investigator…）
一次性动作    →  Commands 或会话简报
```

**Skill 描述字段（`description`）要写明 WHEN**：例如 *「用户要求 push / 大型 PR / 触及 domains/ 时」*，便于 Agent 自动选用，减少人工 `@` 全文。

#### 3.2.6 渐进式检索与文件加载纪律

　　Agent 模式的 token 浪费，多半来自 **探索性读文件**（「我先看看项目结构」读数十个文件）。下列纪律应写入项目 Rule 或 Skill（可参考 [Minimize-Cursor-Cost](https://github.com/inboxpraveen/Minimize-Cursor-Cost)、[cursorskills/dont-be-greedy](https://github.com/bluriesophos/cursorskills)）：

**加载顺序（由便宜到贵）：**

```
1. 读 00-/08- 锚点与域边界表（已在 Rules 中则可跳过）
2. Grep / Glob / SemanticSearch 定位符号与路径
3. Read 目标文件 ± 上下文行（offset/limit），大文件禁止全文
4. 仅在实现同类功能时，Read 1 个参考实现
5. 禁止：无目标地 list 全目录、重复 read 未变文件
```

**Agent 模式硬纪律（建议写入 `.cursor/rules/token-discipline.mdc`）：**

| 纪律 | 说明 |
|------|------|
| 不重复读 | 同一会话内已读且未改的文件不再 `read` |
| 批量工具 | 无依赖的 Grep/Read 并行发起 |
| 范围守卫 | 未说明时默认 **≤3 个文件**；超出先问用户从哪切 |
| 验证分级 | 优先 `read_lints` / 单测；全量 test 仅用户要求或高风险变更 |
| 输出克制 | 先结论后细节；默认 diff 编辑，非整文件重写 |
| 探索预算 | 「理解代码库」类任务：先给 **阅读计划**（≤5 个路径），用户确认后再读 |

**蓝图专用检索地图（示例）：**

| 要问的问题 | 先查哪里 | 再查哪里 |
|------------|----------|----------|
| 能不能改这张表？ | Rules 域边界表 | `domains/*/models` |
| API 放哪？ | `08-` §2.7 | `app/api/*.py` |
| 赛制怎么扩？ | `02-` §5.0、`arena/ARCHITECTURE.md` | `content/game-configs/` |
| 生涯逻辑？ | `06-`、`career/DESIGN.md` | `domains/career/` |
| 为何这样定？ | `docs/decisions/` | — |

### 3.3 In-Session：执行阶段

#### 3.3.1 标准开发顺序

　　在一个涉及前后端的功能开发会话中，推荐遵循以下顺序：

```
1. 契约/接口定义  →  先确定域间通信的形状（API 签名、类型定义）
2. 数据模型/迁移  →  创建或修改数据库表
3. 后端逻辑       →  实现 API 端点和业务逻辑
4. 前端实现       →  对接后端 API，实现 UI
5. 集成验证       →  端到端手测或冒烟测试
```

　　这个顺序的核心逻辑是**依赖方向**：前端依赖后端，后端依赖模型，模型依赖契约。反过来做会导致大量返工。

#### 3.3.2 任务分解启发式

　　当任务复杂度超出单次会话的舒适范围时，需要分解。以下三条经验规则可作为分解触发器：


| 规则          | 阈值                    | 分解策略      |
| ----------- | --------------------- | --------- |
| **3 域规则**   | 单次会话跨超过 3 个域          | 按域拆分为多个会话 |
| **10 文件规则** | 预计改动超过 10 个文件         | 按功能竖切拆分   |
| **30 分钟规则** | 单个子任务需 AI 连续输出超 30 分钟 | 拆为更细的子任务  |


**竖切（Vertical Slice）分解法**：

　　对于大型功能，优先按"用户可感知的最小完整路径"竖切，而非按技术层水平切割。

```
推荐（竖切）：                    不推荐（水平切）：
  会话 1: 创建赛事 → 完整闭环        会话 1: 所有后端 API
  会话 2: 加入赛事 → 完整闭环        会话 2: 所有前端页面
  会话 3: 完赛结算 → 完整闭环        会话 3: 所有集成测试
```

　　竖切的优势是每次会话都产出可验证的端到端成果；水平切则在第三次会话时才能首次验证，且前两次的错误可能已累积。

#### 3.3.3 中间检查点

　　在执行过程中，每完成一个子任务后，应暂停并检查：

- **域边界**：是否不小心在域 A 的代码中操作了域 B 的表？
- **路由归属**：新增的路由是否放在了正确的模块中？
- **类型一致性**：前端类型定义是否与后端响应格式匹配？
- **Phase 越界**：是否在实现过程中"顺手"做了下一个 Phase 的事？

### 3.4 Post-Session：收尾阶段

#### 3.4.1 推送前文档对齐

　　这是蓝图编程最具特色的实践之一：**代码推送前，必须先完成文档与代码的对齐。** 对齐不是可选的装饰，而是强制的质量门。

**对齐清单（通用版）：**

1. **盘点变更**：查看本次会话修改了哪些文件、涉及哪些模块。
2. **更新权威文档**：确保以下信息与代码一致——
  - 目录树/文件结构描述
  - API 路由列表
  - 数据库表清单
  - 功能矩阵 / 实现状态百分比
  - Phase 进展标记
3. **统一元数据**：更新文档的"最后更新"日期；确保路径引用指向当前实际路径。
4. **自检一致性**：文档中声明的内容与代码实际状态无矛盾。
5. **同批推送**：代码提交与文档更新在同一批次内完成，不留时间差。

**触发条件**：

- 用户要求推送到远程仓库
- 本次会话包含架构级变更（新域、新路由、新表、新赛制等）
- 单次提交涉及 5 个以上文件

**豁免条件**：

- 单文件 Bug 修复
- 纯文档 Typo 修正
- 无行为变更的重构

#### 3.4.2 决策日志（ADR）

　　架构决策记录（Architecture Decision Record）解决的是"跨会话的架构记忆"问题。当一次会话做了重要的技术选择时，应在项目中留下简短的决策记录。

**记录触发条件**：

- 选择了 A 方案而非 B 方案
- 引入了新的依赖或技术
- 改变了现有的架构模式
- 为了 trade-off 而接受了某个妥协

**ADR 简要格式**（详见第九章模板）：

```
标题：[决策的一句话描述]
日期：[YYYY-MM-DD]
状态：已采纳 / 已废弃 / 已取代（被 ADR-xxx 取代）
上下文：[为什么需要做这个决策]
决策：[选择了什么方案]
放弃方案：[考虑过但未采用的方案及原因]
后果：[这个决策带来的影响，包括正面和负面]
```

#### 3.4.3 会话偏差回顾

　　会话结束时，快速回顾：

- 计划做的 vs 实际做的，是否有偏差？
- 偏差的原因是什么？（任务估计不准 / AI 偏离 / 需求变更）
- 是否有遗留项需要在下次会话中处理？

　　偏差回顾不需要很正式，一句话记录即可。但如果偏差反复出现相同模式（如 AI 总是在某个域越界），则应回到 L2 强化相应的约束规则。

---

## 第四章 域治理——边界、契约、归属

### 4.1 为什么需要域治理

　　在中大型项目中，"代码放在哪里""数据归谁管""API 归哪个模块"这些看似简单的问题，是架构一致性的基础。在传统开发中，这些知识往往存在于团队成员的脑中；但在 AI 编程中，AI 没有这种隐性知识。域治理就是将这些隐性知识**显式化、声明化**。

### 4.2 域的定义与划分

　　"域"（Domain）是项目中拥有独立职责、独占数据的功能边界。域的划分原则：

1. **高内聚**：域内的代码和数据紧密相关
2. **低耦合**：域间的依赖通过明确的接口而非内部引用
3. **数据独占**：每张表有且只有一个主人域
4. **独立演进**：一个域的内部重构不应破坏其他域

**域划分的常见模式：**


| 模式         | 适用场景      | 示例                    |
| ---------- | --------- | --------------------- |
| **按业务能力**  | 业务边界清晰的应用 | 认证域、赛事域、生涯域           |
| **按数据所有权** | 数据密集型系统   | 用户数据域、交易数据域           |
| **按变更频率**  | 部分模块迭代极快  | 核心引擎域（稳定）vs 赛制域（频繁扩展） |


### 4.3 域边界表

　　域边界表是蓝图编程中最重要的战术契约之一。它用一张表格声明每个域的"领地"。

**模板：**


| 域             | 代码路径                                     | 拥有的表                                | 禁止事项                      |
| ------------- | ---------------------------------------- | ----------------------------------- | ------------------------- |
| **identity**  | `models/user.py` · `api/auth.py`         | `users`                             | 其他域直接改 `users` 字段         |
| **arena**     | `domains/arena/` · `api/competitions.py` | `events` · `participants` · `teams` | 在 `games/` 中直接建/改 arena 表 |
| **games/xxx** | `games/xxx/*.py`                         | `xxx_rounds` · `xxx_decisions`      | 跳过 arena 域直接操作 `events`   |
| **career**    | `domains/career/`                        | `xp_events`                         | 其他域直接改 `user.xp`          |


**设计要点：**

- "禁止事项"列是域边界表的灵魂。它不只告诉 AI "什么归谁"，更告诉 AI "什么是绝对不能做的"。AI 对禁止指令的遵从度高于正面指令。
- 域边界表应放在 AI 工具的自动注入规则中，确保每次会话都能看到。

### 4.4 路由归属矩阵

　　在 Web 应用中，API 路由的归属同样需要显式管理。

**模板：**


| 功能范围      | 归入模块       | 文件路径                |
| --------- | ---------- | ------------------- |
| 认证/用户管理   | auth       | `api/auth.py`       |
| 资源 CRUD   | resources  | `api/resources.py`  |
| 业务操作      | operations | `api/operations.py` |
| WebSocket | ws         | `api/ws.py`         |
| 管理后台      | admin      | `api/admin.py`      |


**核心规则**：禁止创建不在此表中的新路由模块而不先更新文档。这条规则看似简单，但它从根本上防止了 AI 随意新建路由文件导致的架构混乱。

### 4.5 表归属与唯一主人原则

> **每张数据表有且只有一个主人域，只有主人域的代码可以写入该表。其他域必须通过 API 调用或域事件间接操作。**

　　这是域治理中最硬的规则。违反此规则的后果是：

- 数据完整性依赖于分散在多个模块中的隐式约定
- 表结构变更时无法判断影响范围
- AI 会在任何它认为"方便"的地方直接操作表

### 4.6 跨域通信

　　当域 A 需要域 B 的数据或能力时，合法的通信方式：


| 方式                | 适用场景                                                 | 耦合度        |
| ----------------- | ---------------------------------------------------- | ---------- |
| **API 调用**        | 同步请求-响应                                              | 中          |
| **域事件**           | 异步通知（如"赛事结束"事件触发 XP 入账）                              | 低          |
| **共享类型**          | 只共享数据形状（TypeScript interface / Pydantic model），不共享实现 | 最低         |
| ~~直接 import 并写入~~ | ~~方便但危险~~                                            | ~~最高（禁止）~~ |


### 4.7 契约层设计

　　在成熟的蓝图编程实践中，域间接口应有**形式化的契约定义**，而不仅是域边界表中的"禁止事项"。

**契约层的渐进建设路径：**


| 阶段          | 契约形式                               | 适用场景            |
| ----------- | ---------------------------------- | --------------- |
| **Level 0** | 无契约，靠口头约定                          | 原型阶段（不推荐超过 2 周） |
| **Level 1** | 域边界表 + 路由归属矩阵                      | MVP 阶段，团队 1-2 人 |
| **Level 2** | 上述 + TypeScript 共享类型 + Pydantic 模型 | 前后端分离、多端应用      |
| **Level 3** | 上述 + OpenAPI Schema + 契约测试         | 多团队协作、对外 API    |


　　大多数 AI 辅助编程项目在 Level 1-2 之间即可获得良好的治理效果。Level 3 适用于项目规模较大或有外部集成需求的场景。

---

## 第五章 Phase 门控——防止范围蔓延

### 5.1 为什么需要 Phase 门控

　　AI 编程中最常见的失控模式之一是**范围蔓延**（Scope Creep）：人类说"做一个登录页"，AI 顺便实现了 OAuth2 + 2FA + 密码强度检测 + 忘记密码流程。或者人类在做 Phase A 的工作时，AI "未雨绸缪"地为 Phase C 的功能预留了复杂的抽象层。

　　Phase 门控通过显式声明"当前阶段做什么、不做什么"，给 AI 划定时间维度上的边界。

### 5.2 Phase 阶梯设计方法

　　Phase 划分的核心原则是**价值递增、依赖递进**：每个 Phase 交付独立可用的价值，后续 Phase 在前序基础上递增。

**设计步骤：**

1. **列出所有待建功能**，按用户价值和技术依赖排序
2. **识别最小可行产品**（MVP），作为 Phase A
3. **按依赖链延伸**：哪些功能必须在 A 之后才能做？归入 Phase B
4. **标记"诱惑陷阱"**：哪些功能看起来很酷但不紧急？显式推迟

**Phase 阶梯模板：**


| Phase     | 主题   | 可以做                    | 不该做                    |
| --------- | ---- | ---------------------- | ---------------------- |
| **A（当前）** | 核心闭环 | 基础 CRUD · 核心引擎 · 基本 UI | 高级分析 · AI Agent · 性能优化 |
| **B**     | 智能增强 | 规则引擎 · 自动化 · 数据看板      | 自然语言交互 · 知识图谱          |
| **C**     | 智能体  | LLM 集成 · Agent 编排      | 自主学习 · 多模态             |
| **D**     | 生态扩展 | 第三方集成 · 插件系统           | —                      |


### 5.3 AI 默认行为规则

　　将以下规则写入 AI 工具的自动注入配置：

> **若用户未明确指定 Phase，AI 默认只做当前 Phase 范围内的工作。当任务可能涉及下一 Phase 时，AI 应主动询问而非自行推进。**

　　这条规则的本质是将 Phase 门控从"人工记得提醒"升级为"AI 自动遵守的默认行为"。

### 5.4 Phase 推进仪式

　　当一个 Phase 的核心目标达成后，应执行一次"Phase 推进仪式"：

1. **回顾清单**：Phase A 的完成定义是否全部达成？
2. **更新文档**：在路线图文档中标记 Phase A 为"已完成"，Phase B 为"当前"
3. **更新约束规则**：修改 AI 自动注入规则中的"当前 Phase"标记
4. **域表扩展**：如 Phase B 引入新域，更新域边界表
5. **归档决策**：整理 Phase A 期间的 ADR，标记为"已固化"

---

## 第六章 质量保障——从手动到系统化

### 6.1 现有实践的质量缺口

　　在典型的蓝图编程实践中，质量保障往往只有一道门：推送前文档对齐。这虽然比无门要好得多，但它有一个致命弱点——**发现问题的时间太晚**。代码已经写完、功能已经"看起来能用"了，此时才发现域越界或类型不一致，修复成本远高于早期发现。

　　本章旨在建立一套**多级质量保障体系**，从编码时到推送后逐层设防。

### 6.2 四道质量门

```
第一道门          第二道门           第三道门           第四道门
编辑即检查        子任务完成时       推送前             推送后
┌──────────┐   ┌──────────┐   ┌──────────────┐   ┌─────────────┐
│ Lint Gate │ → │ 冒烟测试  │ → │ 文档对齐门    │ → │ 回归守护     │
│ 类型检查  │   │ 域边界核查 │   │ 一致性自检    │   │ CI 流水线    │
└──────────┘   └──────────┘   └──────────────┘   └─────────────┘
```

#### 第一道门：编辑即检查（Lint Gate）

　　每次 AI 完成一段代码编辑后，立即检查 Lint 错误和类型错误。这是成本最低、发现最早的质量门。

**实施方式：**

- 在 AI 规则中写入"每次编辑后检查 Lint"的指令
- 优先修复新引入的 Lint 错误
- 不强求修复历史遗留的 Lint 错误（避免偏离主任务）

#### 第二道门：子任务冒烟测试

　　每完成一个子任务（如实现一个 API 端点），执行最小冒烟测试。

**冒烟测试清单模板：**


| 子任务类型     | 最小冒烟测试                      |
| --------- | --------------------------- |
| 新增 API 端点 | 用 curl / httpie 调用一次，验证响应结构 |
| 新增前端页面    | 浏览器打开，验证渲染无白屏               |
| 数据库迁移     | 启动后端，验证表已创建                 |
| 引擎逻辑      | 跑一个最小用例，验证输出合理              |


#### 第三道门：推送前文档对齐

　　此门在第三章已详述。核心是代码与文档的一致性检查。

#### 第四道门：回归守护（渐进建设）

　　对于核心路径（如创建赛事 → 加入 → 完赛 → 结算），应建立端到端测试。这不需要一步到位，可以渐进建设：


| 阶段     | 测试形式                            | 投入        |
| ------ | ------------------------------- | --------- |
| **初期** | 手动冒烟测试清单（Markdown）              | 极低        |
| **中期** | 关键路径 API 测试（pytest / supertest） | 中等        |
| **成熟** | CI 流水线自动运行                      | 较高（但一劳永逸） |


### 6.3 错误恢复协议

　　AI 犯错是常态，而非例外。关键是有标准化的恢复流程，而非每次临时应对。

**错误分级与恢复策略：**


| 等级          | 表现              | 恢复策略                                 |
| ----------- | --------------- | ------------------------------------ |
| **L1 语法错误** | Lint/类型报错       | AI 即时修复；无需人工介入                       |
| **L2 逻辑错误** | 功能行为不符合预期       | 定位原因，修复后重新冒烟测试                       |
| **L3 架构越界** | 跨域写入、路由错放、表归属错误 | **停止编码**，回到文档重新核对约束，再修正              |
| **L4 数据破坏** | 错误的迁移删除了数据列     | `git stash` / `git checkout` 回退到安全状态 |


**L3/L4 错误的标准处理流程：**

1. **停止**——不要让 AI 继续在错误基础上叠加更多代码
2. **诊断**——确认错误的根因（约束遗漏？AI 幻觉？需求理解偏差？）
3. **回退**——使用 Git 回退到最近的正确状态
4. **强化约束**——如果是约束遗漏导致的错误，在 L2 层补充相应规则
5. **重做**——在强化后的约束下，让 AI 重新实现

### 6.4 文档自检的自动化思路

　　文档与代码的一致性检查，在理想情况下可以部分自动化：


| 检查项              | 自动化方式                         |
| ---------------- | ----------------------------- |
| 目录树描述 vs 实际目录    | 脚本扫描目录结构，与文档中的树状图对比           |
| API 列表 vs 实际路由   | 从框架（如 FastAPI）自动提取已注册路由，与文档对比 |
| 表清单 vs 实际 Schema | 从 ORM 模型自动提取表名，与文档域边界表对比      |
| Phase 进展 vs 实际功能 | 需要人工判断，难以完全自动化                |


　　即使不做完全自动化，仅仅把"应该检查什么"列成清单，也比依赖记忆要可靠得多。

---

## 第七章 反模式与失败图谱

### 7.1 概述

　　本章归档了 AI 辅助编程中反复出现的失败模式。每个反模式包含：名称、症状、根因、预防措施和修复方法。了解这些模式后，可以在约束体系中预防性地加入对应的护栏。

### 7.2 反模式清单

#### AP-1 上下文溢出（Context Overflow）


| 维度     | 描述                                                  |
| ------ | --------------------------------------------------- |
| **症状** | 会话后半段 AI 产出的代码与前半段矛盾；AI "忘记"了早期的约定                  |
| **根因** | 单次会话注入了过多文档或编写了过多代码，超出 AI 的有效注意力范围                  |
| **预防** | 遵守上下文注入策略（4-5 份文档上限）；大任务竖切；遵守 3 域 / 10 文件 / 30 分钟规则 |
| **修复** | 开新会话，只注入当前子任务需要的最小文档集                               |


#### AP-2 幻觉路径（Hallucinated Paths）


| 维度     | 描述                                     |
| ------ | -------------------------------------- |
| **症状** | AI 引用不存在的文件路径、import 不存在的模块、调用不存在的 API |
| **根因** | AI 从训练知识推断路径，而非从项目实际结构                 |
| **预防** | 在约束规则中要求 AI "编辑前先读取文件确认其存在"；提供准确的目录树   |
| **修复** | 纠正路径后，在域边界表或目录树文档中补充正确路径               |


#### AP-3 跨域渗透（Cross-Domain Penetration）


| 维度     | 描述                                 |
| ------ | ---------------------------------- |
| **症状** | 域 A 的代码中出现了对域 B 表的直接读写             |
| **根因** | AI 倾向于走最短路径实现功能，直接操作比通过 API 调用"更快" |
| **预防** | 域边界表中明确"禁止事项"列；AI 自检协议中包含域边界检查     |
| **修复** | 移除跨域直接操作，改为 API 调用或域事件             |


#### AP-4 Mock 泄漏（Mock Leakage）


| 维度     | 描述                                    |
| ------ | ------------------------------------- |
| **症状** | 正式代码中混入了 mock 数据或硬编码的假数据，而非对接真实 API   |
| **根因** | AI 在示例代码或初期 scaffold 中使用 mock，后续忘记替换  |
| **预防** | 在约束规则中明确"新功能必须对接后端 API，不得新增 mock 数据源" |
| **修复** | 搜索项目中的 mock 数据文件，逐一替换为真实 API 调用       |


#### AP-5 注释膨胀（Comment Bloat）


| 维度     | 描述                                          |
| ------ | ------------------------------------------- |
| **症状** | 代码中充斥着 `// 导入模块`、`// 定义函数`、`// 返回结果` 等叙述性注释 |
| **根因** | AI 默认倾向于为每一步操作添加解释性注释                       |
| **预防** | 在编码风格规则中明确"只注释非显而易见的意图和约束，不写叙述性注释"          |
| **修复** | 批量清理叙述性注释，只保留解释"为什么"而非"做了什么"的注释             |


#### AP-6 编码/Shell 陷阱（Encoding/Shell Trap）


| 维度     | 描述                                                                |
| ------ | ----------------------------------------------------------------- |
| **症状** | 在 Windows PowerShell 中执行了 Bash 语法的命令（如 `&&`、`export`、heredoc）导致失败 |
| **根因** | AI 的训练数据以 Linux/Mac 为主，默认生成 Bash 命令                               |
| **预防** | 在用户级规则中声明操作系统和 Shell 类型；要求 AI 使用对应语法                              |
| **修复** | 改用正确的 Shell 语法；复杂命令写入脚本文件执行                                       |


#### AP-7 文档漂移（Documentation Drift）


| 维度     | 描述                                   |
| ------ | ------------------------------------ |
| **症状** | 文档声明"X 功能待建"，但代码中 X 已经实现；或文档路径与实际不匹配 |
| **根因** | 代码变更后未同步更新文档；推送前对齐步骤被跳过              |
| **预防** | 推送前文档对齐门（第三道质量门）；在 AI 规则中设为强制步骤      |
| **修复** | 全量核对文档与代码的一致性，补齐差异                   |


#### AP-8 过度抽象（Premature Abstraction）


| 维度     | 描述                                           |
| ------ | -------------------------------------------- |
| **症状** | AI 为只有一个实现的接口创建了复杂的抽象层、工厂模式、策略模式             |
| **根因** | AI 倾向于"最佳实践"式编码，为未来可能性过度设计                   |
| **预防** | Phase 门控限制当前阶段的复杂度上限；明确告诉 AI "YAGNI（你不会需要它）" |
| **修复** | 简化为直接实现；如果未来确实需要抽象，届时再重构                     |


#### AP-9 悄然依赖（Silent Dependency）


| 维度     | 描述                                                                            |
| ------ | ----------------------------------------------------------------------------- |
| **症状** | AI 引入了 `requirements.txt` 或 `package.json` 中不存在的新依赖，运行时才报 ModuleNotFoundError |
| **根因** | AI 在生成代码时使用了训练数据中的库，但未更新依赖清单                                                  |
| **预防** | 在编码规则中要求"引入新依赖时必须同步更新依赖管理文件"                                                  |
| **修复** | 安装缺失依赖并更新依赖清单；或替换为项目已有的库                                                      |


#### AP-10 贪婪加载（Greedy Context Load）


| 维度     | 描述                                                         |
| ------ | ---------------------------------------------------------- |
| **症状** | 会话未动手先读 10+ 文件；或把 500 行文件全文读入；token 在「探索」阶段耗尽 |
| **根因** | Agent 默认「先理解全貌」；缺少加载预算与检索顺序                              |
| **预防** | §3.2.6 渐进式检索；`token-discipline.mdc`；大任务先出阅读计划              |
| **修复** | 新会话 + 最小文档集 + 仅打开任务相关文件                                   |


#### AP-11 规格漂移（Spec Drift in Session）


| 维度     | 描述                                              |
| ------ | ----------------------------------------------- |
| **症状** | 聊嗨了偏离会话简报；实现 Phase C 功能；与 L1 文档矛盾               |
| **根因** | 纯 vibe 式对话无边界；未在检查点对照简报                          |
| **预防** | 会话简报 + 子任务检查点（§3.3.3）；Phase 默认规则                 |
| **修复** | 停止编码；更新简报或 ADR；从竖切最小路径重做                        |


### 7.3 反模式 → 约束映射

　　每个反模式都可以通过在约束体系中添加对应的预防规则来系统性地降低发生概率：


| 反模式          | 应在哪一层增加约束   | 约束形式           |
| ------------ | ----------- | -------------- |
| AP-1 上下文溢出   | L3 会话控制     | 注入上限 + 分解规则    |
| AP-2 幻觉路径    | L3 会话控制     | "先读后写"规则       |
| AP-3 跨域渗透    | L2 域边界表     | 禁止事项列          |
| AP-4 Mock 泄漏 | L2 编码规范     | "禁止新增 mock"规则  |
| AP-5 注释膨胀    | L2 编码风格     | 注释策略规则         |
| AP-6 编码陷阱    | L3 用户规则     | OS/Shell 声明    |
| AP-7 文档漂移    | L3 推送门      | 强制对齐流程         |
| AP-8 过度抽象    | L2 Phase 门控 | 当前 Phase 复杂度上限 |
| AP-9 悄然依赖    | L2 编码规范     | 依赖同步规则         |
| AP-10 贪婪加载   | L3 + Rules  | `token-discipline.mdc` |
| AP-11 规格漂移   | L3 会话简报     | 子任务检查点 + Phase 规则 |


---

## 第八章 多 Agent 协作

### 8.1 从单 Agent 到多 Agent

　　随着 AI 编程工具的进化（如 Cursor 的子代理、并行 Agent、后台 Agent），单次会话中可以同时运行多个 AI 实例。这带来了效率的飞跃，也引入了新的协作挑战。

　　蓝图编程的约束体系天然适合多 Agent 场景——域边界表和路由归属矩阵为每个 Agent 划定了清晰的"施工区域"。

### 8.2 Agent 角色分类


| 角色                   | 权限       | 适用场景           |
| -------------------- | -------- | -------------- |
| **探索者（Explorer）**    | 只读       | 搜索代码、理解架构、回答问题 |
| **执行者（Implementer）** | 读写       | 编写代码、修改文件      |
| **审查者（Reviewer）**    | 只读       | 检查代码质量、域边界合规性  |
| **运维者（Operator）**    | Shell 执行 | 运行命令、部署、Git 操作 |


### 8.3 并行 Agent 的任务划分策略

**安全的并行模式：**


| 模式          | 描述                                   | 前提条件          |
| ----------- | ------------------------------------ | ------------- |
| **按域并行**    | Agent A 做 arena 域，Agent B 做 career 域 | 两个域无共享文件      |
| **探索 + 执行** | Agent A（只读）搜集信息，Agent B 基于结果编码       | A 完成后 B 才开始写入 |
| **前端 + 后端** | Agent A 做后端 API，Agent B 做前端页面        | 先约定 API 接口签名  |


**危险的并行模式（应避免）：**


| 模式             | 风险              | 替代方案                  |
| -------------- | --------------- | --------------------- |
| **同域并行写入**     | 文件冲突、逻辑矛盾       | 改为串行，或拆为更细的不重叠子任务     |
| **无契约的前后端并行**  | 类型不匹配、API 签名不一致 | 先用一个 Agent 定义接口契约，再并行 |
| **共享配置文件并行修改** | 合并冲突            | 一个 Agent 负责配置文件       |


### 8.4 Agent 间上下文传递

　　多个 Agent 之间的上下文传递是多 Agent 协作的关键难点。推荐的传递策略：

1. **通过文件传递**——Agent A 的产出（代码、文档）自动成为 Agent B 的上下文。这是最自然的方式。
2. **通过任务描述传递**——启动 Agent B 时，在任务描述中包含 Agent A 的结论摘要。
3. **通过共享文档传递**——多个 Agent 都读取同一份约束文档（如域边界表），确保行为一致。

### 8.5 合并冲突的预防

　　预防优于修复。以下措施可大幅降低多 Agent 场景下的合并冲突：

- **文件级隔离**：确保并行 Agent 不修改同一个文件
- **仅追加模式**：对于共享的注册文件（如路由注册、类型导出），采用仅追加而非修改的方式
- **锁定共享资源**：对于必须修改的共享文件（如 `main.py` 的路由挂载），指定唯一 Agent 负责
- **最后合并**：各 Agent 完成独立工作后，用一个专门的会话进行集成和冲突处理

### 8.6 与 Skills / 子代理工作流（Superpowers 类）

　　社区成熟框架（如 [obra/superpowers](https://github.com/obra/superpowers)）将 **规划 → 执行 → 验证 → 收尾** 拆为可组合 Skills，与蓝图编程高度互补：

| Superpowers 类 Skill | 蓝图编程落点 |
|---------------------|-------------|
| `brainstorming` / `writing-plans` | 会话简报 + L1 文档更新（非直接写码） |
| `executing-plans` / `subagent-driven-development` | 竖切 + 探索者/执行者分工（§8.2） |
| `test-driven-development` | 第六章第二道门（冒烟 → 单测） |
| `systematic-debugging` | L3 错误恢复协议（§6.3） |
| `verification-before-completion` | 完成定义 checklist |
| `finishing-a-development-branch` | Post-Session：文档对齐 + ADR |

　　**集成原则**：Superpowers 管 **怎么做一步**；蓝图管 **在哪做、做到哪一 Phase**。在 Cursor 中可通过 `/add-plugin superpowers` 或项目 `.cursor/skills/` 引入；**勿**让 Skills 中的流程覆盖本仓库域边界与 Phase 门控。

---

## 第九章 落地模板包

### 9.1 项目启动 Checklist

　　从零搭建蓝图编程体系时的检查清单：

```markdown
## 蓝图编程体系搭建 Checklist

### L1 战略蓝图
- [ ] 产品愿景文档：定义了平台定位、目标用户、核心价值
- [ ] 技术架构文档：选定技术栈、声明架构原则和硬约束
- [ ] 阶段路线图：划分了 Phase 阶梯，标记当前 Phase
- [ ] 文档体系目录：所有文档有编号、有"定位"首行

### L2 战术契约
- [ ] 域边界表：列出所有域、代码路径、拥有的表、禁止事项
- [ ] 路由归属矩阵：所有 API 模块和归属规则
- [ ] Phase 门控表：当前 Phase 的"可以做 / 不该做"
- [ ] 编码风格约定：命名、类型、注释、错误处理
- [ ] 约束规则文件（.cursor/rules/ 或等效）：自动注入关键规则

### L3 会话控制
- [ ] 会话简报模板：写入 README 或开场指南
- [ ] 上下文注入清单：按任务类型的文档组合
- [ ] AI 自检协议：5 项强制确认
- [ ] 推送前对齐清单：文档 + 代码一致性检查步骤
- [ ] 决策日志目录（可选）：`docs/decisions/` 或等效

### 持续维护
- [ ] 文档"最后更新"日期机制
- [ ] Phase 推进仪式流程
- [ ] 约束规则的定期审查计划（如每 Phase 回顾一次）
```

### 9.2 文档体系骨架

　　以下是一个通用的文档体系骨架，可根据项目规模裁剪：

```
project-root/
├── README.md                    入口 + AI 编程会话开场指南
├── 00-项目全景与目录结构.md       仓库地图 + 文档搭配关系
├── 01-产品愿景与架构.md           产品定位 + 域定义 + 核心价值
├── 02-[领域]体系与产品.md         核心业务领域详述
├── 03-技术架构与实现现状.md       技术栈 + 域分包 + AI 编程方法论
├── 04-实施路线与里程碑.md         Phase 阶梯 + 成本估算
├── 05-[可选专题].md              大型专题（如有）
├── 06-生涯模式.md                 Career Hub 产品权威（原索引并入 README/00-）
├── 07-拟真城市阅读合集.md          终局出口 B（与 05- OPC 并列）
├── 08-工程现状与实现详表.md       API/模型/百分比/变更记录
├── 09-协作流程.md                 竖切 + AI 编程上下文注入
├── .cursor/rules/                短约束（alwaysApply 宜 ≤5 条）
│   ├── blueprint-coding.mdc     域边界 + 路由归属 + Phase 门控
│   ├── docs-align-before-push.mdc（或改为 skills 按需加载）
│   └── token-discipline.mdc     检索与 token 纪律（§9.5）
├── .cursor/skills/               多步工作流（§9.6、附录 C）
└── docs/decisions/               架构决策记录（ADR）
    └── 001-[决策标题].md
```

**注意**：编号不必连续（如跳过 07），但应保持**一致的编号风格**。建议预留编号空间，避免后期需要插入时打乱序列。

### 9.3 AI 编程约束规则模板

　　以下是一份可直接复用的约束规则模板（适用于 Cursor `.cursor/rules/` 或类似机制）：

```markdown
---
description: [项目名] 蓝图编程规范
alwaysApply: true
---

# 蓝图编程规范

本项目主要依赖 AI 辅助编程。[文档体系] 是 AI 编程会话的权威上下文锚点。
所有代码产出须在文档定义的框架内，不得偏离。

## 1 域边界（硬性）

| 域 | 代码路径 | 拥有的表 | 禁止 |
|----|----------|----------|------|
| [domain-a] | [path] | [tables] | [prohibited actions] |
| [domain-b] | [path] | [tables] | [prohibited actions] |

## 2 路由归属

| 范围 | 归入 | 文件 |
|------|------|------|
| [scope] | [module] | [file path] |

禁止创建不在此表中的新路由模块而不先更新文档。

## 3 Phase 优先级

当前处于 **Phase [X]**。

| 优先级 | 可以做 | 不该做 |
|--------|--------|--------|
| Phase [X]（当前） | [items] | — |
| Phase [X+1] | [items] | [premature items] |

AI 编程会话中，若用户未明确指定，默认只做当前 Phase 范围内的工作。

## 4 编码风格

- [语言]：[框架] + [风格约定]
- 注释：只注释非显而易见的意图和约束
- 命名：[后端命名] / [前端命名] / [数据库命名]
- 错误处理：[后端策略] / [前端策略]

## 5 会话自检

每次编程会话开始时，AI 应确认：
1. 本次任务属于哪个 Phase？
2. 涉及哪些域？是否存在跨域写入风险？
3. 是否需要新增路由？归入哪个模块？
4. 是否需要新建表？主人域是谁？
5. [项目特有的检查项]

若任一项有疑问，应向用户确认后再动手。
```

### 9.4 推送前文档对齐规则模板

```markdown
---
description: 大型更新后推送前文档对齐
alwaysApply: true
---

# 推送前文档对齐（强制）

## 触发条件（满足任一即执行）

- 用户明确要求推送到远程
- 本次会话含架构级变更
- 单次提交 ≥5 个文件

## 步骤

1. 盘点代码变更
2. 对齐权威文档（按变更范围择要更新）
3. 统一元数据（日期、路径）
4. 自检一致性
5. 同批提交再推送

## 禁止

- 声称"已对齐"但未修改文档文件
- 用口头描述代替实质的文件修改
- 推送后才发现不一致
```

### 9.5 会话简报模板

```markdown
## 本次边界
- 任务：[一句话描述要做什么]
- Phase：[当前 Phase 编号]
- 涉及域：[列出域名，如 arena + games/trading]
- 禁止：[明确不可触及的域/表/文件]

## 完成定义
- [ ] [可验证的条件 1]
- [ ] [可验证的条件 2]
- [ ] [可验证的条件 3]

## 上下文（按需 attach）
- [文档 1 名称] — [为什么需要]
- [文档 2 名称] — [为什么需要]
```

### 9.6 架构决策记录（ADR）模板

```markdown
# ADR-[序号]: [决策标题]

**日期**：YYYY-MM-DD
**状态**：已采纳 | 已废弃 | 已取代（被 ADR-xxx 取代）

## 上下文

[描述面临的问题或需要做的决策，1-3 句话]

## 决策

[选择了什么方案，1-3 句话]

## 考虑过的方案

| 方案 | 优点 | 缺点 | 未采用原因 |
|------|------|------|-----------|
| [方案 A] | [优点] | [缺点] | [原因] |
| [方案 B（已采纳）] | [优点] | [缺点] | — |

## 后果

- **正面**：[这个决策带来的好处]
- **负面/代价**：[这个决策带来的 trade-off]
- **需要关注**：[未来可能需要重新审视的条件]
```

### 9.7 Phase 门控表模板

```markdown
# Phase 门控表

**当前阶段**：Phase [X] — [阶段主题]
**起始日期**：YYYY-MM-DD
**完成条件**：[列出必须达成的条件]

## Phase 阶梯

| Phase | 主题 | 可以做 | 不该做 | 状态 |
|-------|------|--------|--------|------|
| A | [主题] | [事项列表] | — | 已完成 ✅ |
| B（当前） | [主题] | [事项列表] | [过早事项] | 进行中 🔵 |
| C | [主题] | [事项列表] | [过早事项] | 未开始 ⚪ |

## Phase B 完成定义

- [ ] [必须达成的条件 1]
- [ ] [必须达成的条件 2]
- [ ] [必须达成的条件 3]

## Phase 推进决策

推进到下一 Phase 前须完成：
1. 回顾本 Phase 完成定义是否全部达成
2. 更新路线图文档标记
3. 更新约束规则中的"当前 Phase"
4. 扩展域边界表（如有新域）
5. 归档本 Phase 的 ADR
```

### 9.5 Token 纪律规则模板（`token-discipline.mdc`）

```markdown
---
description: Agent 模式下的上下文与检索纪律，降低 token 消耗
alwaysApply: true
---

# Token 与检索纪律

- 同一会话不重复 read 未变更文件
- 大文件用 offset/limit；禁止无目标全库扫读
- 先 Grep/SemanticSearch 定位，再 Read
- 默认单次改动 ≤3 个文件；超出先确认范围
- 验证：优先 read_lints / 单测；全量 test 仅用户要求
- 回复：先结论；编辑用最小 diff
- 权威文档：按任务只 @ 00～09 中 4～5 份相关章节，非全文
```

### 9.6 项目 Skill 模板（推送前对齐）

```markdown
---
name: docs-align-before-push
description: >-
  用户要求 git push、或大型架构/域/API 变更、或单次 ≥5 文件时。
  先对齐 00～09 与 08- §5.1，再提交推送。
---

# 推送前文档对齐

1. `git status` + `git diff --stat` 盘点影响
2. 按变更更新 README/00-/08-（及 02-/04-/09- 若触及）
3. 统一「最后更新」日期；路径与 main.py 路由一致
4. 自检：无「待建」已落地仍标红
5. 代码与文档同一推送批次
```

---

## 附录 A · businessmatch-v1 赛事分层（仓库专用）

> **权威详述**：[02-赛事体系与双端产品.md](./02-赛事体系与双端产品.md) §5.0 · [arena/ARCHITECTURE.md](./webapp/backend/app/domains/arena/ARCHITECTURE.md)

　　本仓库商业模拟赛事采用四层模型，AI 会话与 PR 须先对齐层级再改代码：

| 层 | 标识 | 职责 |
|----|------|------|
| 引擎 | `engine` in YAML → `app/games/<engine>/` | 结算内核（稳定，少 fork） |
| 配置 ID | `game_config_id` → `content/game-configs/<id>.yaml` | 规则变体：练习包、国内正式、地区正式（如美/欧） |
| 场次类型 | `match_kind`：`practice` / `official` | 办赛流程、XP 权重、房间码与控场 |
| 流程代码 | `practice.py` / `*_admin` / lobby 等 | 生命周期与安全，不复制引擎 |

　　**口诀**：引擎一个；配置 ID 管「怎么玩」；`match_kind` 管「怎么办」；交付名写清「\<赛制\> · 练习」或「\<赛制\> · 正式赛」。

---

## 附录 B · 生涯模式与编号空间（仓库专用）

> **权威详述**：[06-生涯模式-大循环家园与资源经济.md](./06-生涯模式-大循环家园与资源经济.md) · [career/DESIGN.md](./webapp/backend/app/domains/career/DESIGN.md)

| 路径 | 含义 |
|------|------|
| **根目录 `06-`** | 生涯模式产品（大循环、T0～T3、家园、NPC） |
| **根目录 `07-`** | 拟真城市 / World 终局（与 06- **无关**） |
| **`inspire/07-`** | 教学内容生成规范（又一独立编号空间） |

　　改生涯功能时 attach **06-** + **career/DESIGN**；改城市母本时 attach **根目录 07-**。

---

## 附录 C · 开源 Skills / Rules 参考

　　以下为 2025～2026 年社区中 **质量较高、与蓝图编程可组合** 的开源资源。使用前请阅读各仓库 LICENSE；生产项目建议 **fork 后裁剪**，只引入与本仓库栈（FastAPI / React / Cursor）相关的条目，避免 `alwaysApply` 规则堆叠过多。

### C.1 方法论与上下文工程

| 仓库 | 星标量级 | 用途 | 链接 |
|------|----------|------|------|
| **Agent Skills for Context Engineering** | ~16k | 上下文退化、压缩、评估；skills 可迁入 `.cursor/skills/` | https://github.com/muratcankoylan/Agent-Skills-for-Context-Engineering |
| **EnzeD/vibe-coding** | — | GDD + Implementation Plan + Memory Bank；与 L1 竖切计划同源 | https://github.com/EnzeD/vibe-coding |
| **obra/superpowers** | 高 | Agentic 工作流：规划、TDD、调试、Code Review、并行子代理 | https://github.com/obra/superpowers |
| **kumekay/cursorpowers** | — | Superpowers 的 Cursor 适配 fork | https://github.com/kumekay/cursorpowers |

### C.2 Rules 合集与工程规范

| 仓库 | 用途 | 链接 |
|------|------|------|
| **PatrickJS/awesome-cursorrules** | 最大的 `.cursorrules` / `.mdc` 策展列表（按语言/框架筛选） | https://github.com/PatrickJS/awesome-cursorrules |
| **d-padmanabhan/cursor-engineering-rules** | 多语言 rules + **015-context-engineering.mdc** + MCP | https://github.com/d-padmanabhan/cursor-engineering-rules |
| **girijashankarj/cursor-handbook** | Rules + Skills + Commands 分层；宣称显著省 token 的命令替代全量 test | https://github.com/girijashankarj/cursor-handbook |

### C.3 Token 节约与 Agent 纪律

| 仓库 | 用途 | 链接 |
|------|------|------|
| **inboxpraveen/Minimize-Cursor-Cost** | Agent 不重复读、批量工具、范围守卫、响应侧精简 | https://github.com/inboxpraveen/Minimize-Cursor-Cost |
| **bluriesophos/cursorskills** | Skills vs Rules 分工、`dont-be-greedy`、`breadcrumbs` 跨会话 | https://github.com/bluriesophos/cursorskills |

### C.4 与本仓库已有约束的对照

| 本仓库已有 | 可参考的开源增强 |
|------------|------------------|
| `.cursor/rules/blueprint-coding.mdc` | `cursor-engineering-rules` 的 workflow / git 规则 |
| `.cursor/rules/docs-align-before-push.mdc` | 可抽为 `.cursor/skills/docs-align-before-push/SKILL.md` 供按需加载 |
| `.cursor/rules/adr-writing.mdc` | Superpowers `writing-plans` + 本仓库 `docs/decisions/` |
| `README` §按角色选读 | `cursor-handbook` 的 context layering 思路 |
| 无 `token-discipline` | 建议新增 §9.5 模板或引入 `Minimize-Cursor-Cost` 片段 |

### C.5 选用建议（避免规则打架）

1. **alwaysApply 总数控制在 3～5 条**（域边界、Phase、token 纪律、OS/语言、推送对齐二选一常驻或改 Skill）
2. **长流程进 Skills**，短禁令进 Rules
3. **不要**同时安装多套「全局编码规范」——保留本仓库 `blueprint-coding.mdc` 为最高优先级
4. 引入外部 rule 后跑 **1 次真实任务**，观察是否触发 AP-10（贪婪读）或 AP-11（偏离 Phase）

### C.6 学术与行业参考（非代码）

| 资料 | 说明 |
|------|------|
| Karpathy, 2025 | Vibe coding 原始表述（X/Twitter） |
| [arXiv:2506.23253](https://arxiv.org/html/2506.23253v1) | Vibe coding 学术综述与风险 |
| Cursor 官方文档 | Rules、Skills、Subagents、Plan 模式 | https://docs.cursor.com |

---

## 附录 术语表


| 术语                                    | 定义                                         |
| ------------------------------------- | ------------------------------------------ |
| **蓝图编程（Blueprint Coding）**            | 以声明式文档体系为约束层，引导 AI 在人类预定义的架构边界内高效产出代码的工程范式 |
| **约束层（Constraint Layer）**             | 由结构化文档组成的规则体系，自动或手动注入 AI 编程会话              |
| **域（Domain）**                         | 项目中拥有独立职责、独占数据的功能边界                        |
| **域边界表（Domain Boundary Table）**       | 声明每个域的代码路径、拥有的表、禁止事项的表格                    |
| **Phase 门控（Phase Gate）**              | 按项目阶段划分"可以做"和"不该做"的优先级阶梯                   |
| **会话简报（Session Brief）**               | 每次 AI 编程会话开始时，人类主动声明的任务边界文档                |
| **上下文注入（Context Injection）**          | 根据任务类型选择性地向 AI 提供相关文档的策略                   |
| **推送前对齐（Pre-Push Alignment）**         | 代码推送前，强制检查文档与代码一致性的质量门                     |
| **ADR（Architecture Decision Record）** | 记录关键架构决策及其上下文、方案对比、后果的轻量文档                 |
| **竖切（Vertical Slice）**                | 按用户可感知的完整功能路径分解任务，而非按技术层分解                 |
| **反模式（Anti-pattern）**                 | 看似合理但实际会导致问题的常见做法                          |
| **战略蓝图（Strategic Blueprint）**         | 三层约束架构的顶层，包含愿景、技术架构、阶段路线图                  |
| **战术契约（Tactical Contract）**           | 三层约束架构的中层，包含域边界、API 契约、Phase 门控等           |
| **会话控制（Session Control）**             | 三层约束架构的底层，包含会话简报、自检、推送对齐等                  |
| **Vibe Coding**                          | 以自然语言驱动 Agent 快速迭代代码的原型式工作流；需与架构约束结合使用   |
| **Agentic Engineering**                  | 在 Agent 杠杆下保持规格、测试与审查的专业智能体工程范式            |
| **Context Engineering**                  | 策划进入模型上下文的高信号 token 集合，对抗注意力衰减与上下文退化      |
| **Rules（.mdc）**                         | Cursor 自动注入的持久约束，宜短、宜分文件、宜 glob  scoped      |
| **Skills（SKILL.md）**                    | 按需加载的多步骤工作流包，含 description 触发条件              |
| **渐进式检索**                              | Grep → 局部 Read → 参考单文件，禁止无预算全库探索              |


---

> **后记**
>
> 　　蓝图编程不是一套死板的教条，而是一个**随项目演进而迭代的约束框架**。其核心洞见很简单：AI 是强大的执行者，但需要人类做好架构师。当你投入时间设计清晰的文档约束体系时，AI 的生产力会成倍放大；当你跳过约束直接让 AI 写代码时，短期的效率终将被混乱的架构吞噬。
>
> 　　好的蓝图编程实践应该是**轻量、渐进、可迭代的**——从最小的域边界表和 Phase 门控开始，随着项目成长逐步补充契约层、决策日志、质量门。完美的约束体系不存在，但持续改进的约束体系胜过没有约束的蛮力编程。

