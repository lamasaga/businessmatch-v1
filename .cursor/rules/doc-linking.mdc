---
description: 文档链接纪律 — 仅权威文档可链入 inspire/；inspire 文内禁止互链，避免外链维护地狱
globs: "**/*.md"
alwaysApply: true
---

# 文档链接纪律（Doc Linking）

## 1 原则

- **编写代码时的事实与契约**：根目录 **`00～04` + `11-`**、`agent.md`（导航）、`docs/decisions/`、`**/DESIGN.md`。
- **`inspire/`**：构想、教研、专题蓝图；**可超前于代码**，不构成实现权威。
- **链接维护成本**集中在少数权威文档，**禁止**在 `inspire/` 内编织大量横向外链。

## 2 谁可以链入 `inspire/`

**允许**在 Markdown 中使用指向 `inspire/` 的相对链接的文档：

| 类别 | 路径 |
| ---- | ---- |
| 项目第一入口 | `agent.md` |
| 对外门户 | `README.md` |
| 权威系列 | `00-` … `04-`、`11-`（根目录编号文档） |

**不允许**（须改为文字指称，无 `](./inspire/...)` 链接）：

- `inspire/**/*.md` → 其他 `inspire/**/*.md`
- `webapp/**`、`OPC/**`、`PPT/**` → `inspire/`（应改链到 `00-PROJECT`/`03-ENGINEERING` 或 `00-PROJECT` §inspire 索引）
- `docs/decisions/*.md` → `inspire/`（ADR 只链 `00～04` 与代码路径）

## 3 `inspire/` 文内允许链什么

| 允许链向 | 示例 |
| -------- | ---- |
| 根目录 `00～04` | `[01-PRODUCT](../01-PRODUCT.md)` |
| `agent.md` | `[agent.md](../agent.md)` |
| `webapp/` 工程契约 | `[career/DESIGN.md](../webapp/backend/app/domains/career/DESIGN.md)` |
| `docs/decisions/` | `[ADR-005](../docs/decisions/005-浮生记RTS调度器单写者.md)` |
| 本目录 **内容资产** `a～f/` | 商赛 YAML 母本、课程单元等同仓库资源路径 |

**禁止**：`inspire/80-` 内 `[81-](./81-xxx.md)` 这类 **inspire 互链**。

**替代写法**：`见 **81-商域 AI 赋能六支柱全景**（inspire 目录登记于 50- §…）`。

## 4 索引维护点

| 维护内容 | 维护位置 |
| -------- | -------- |
| inspire 专题**对外入口**（新增 50+ 稳定稿） | [`00-PROJECT` §inspire 索引](./00-PROJECT.md#inspire-专题索引) **+** [`inspire/50-`](./inspire/50-目录导航.md) |
| inspire **库内目录** | 仅 `inspire/50-`（及 `a～f` 子目录自有 README） |
| 实现度、API | 仅 `03-ENGINEERING.md` |

　　新增 inspire 专题时：**不要**在多个权威文档重复长链；优先只改 `00-PROJECT` §inspire 索引 一行 + `50-` 一行。

## 5 AI 执行

- 润色 `inspire/` 时：发现互链则改为**编号指称**。
- 新建权威文档需要指向构想时：链到 `00-PROJECT` §inspire 索引 或具体 `inspire/NN-`（权威文档内）。
- 用户要求「全仓批量改链」时：先确认是否触及 `inspire/` 互链清理范围。
