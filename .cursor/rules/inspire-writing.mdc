---
description: inspire 与构想类文档的叙述文风 — 充分阐述目的与意图，避免过度精简、名词堆砌与刻意压缩分析
globs: inspire/**/*.md
alwaysApply: false
---

# Inspire / 构想文档撰写规范

　　本规则约束 **`inspire/`** 及同类**方向性、框架性、启发性**文档的写作方式。这类文稿的首要读者是**主理人、产品、教研、投资人、未来协作者**；其次才是 AI 编程会话。因此：**不必为省 Token 而牺牲可读性与论证完整性**。

---

## 1 写之前先判断「文档层级」

| 层级 | 典型路径 | 文风要求 |
| ---- | -------- | -------- |
| **工程契约 / 实现锚点** | 根目录 `00～09`（尤其 `08-`）、`**/DESIGN.md`、`docs/decisions/` | 可精炼；表格、清单、路径引用为主；叙述段简短即可 |
| **构想 / 产品论述 / 教学蓝图** | `inspire/**/*.md`、部分 `05-`/`06-`/`07-` 的产品章节 | **必须有充分叙述段**；说清楚目的、意图、取舍与边界 |
| **对外演示 / PPT** | `PPT/**` | 视觉为主；正文可短，但关键句须完整、自然 |

　　**禁止**把 `inspire/` 写成「API 速查」「术语表」或「只有 bullet 的提纲」。清单与表格是**论证的收束**，不是全文主体。

---

## 2 必须写清楚的内容（叙述段，不可省略）

　　每一**一级章节**（`##`）开头，至少包含 **2～4 段**叙述性正文（段首按中文排版习惯使用两个全角空格），说明：

1. **这一节要解决什么问题**（读者带着什么疑问来）
2. **为什么现在要做 / 为什么这样划分**（动机与背景）
3. **与上下游的关系**（链到 `00～09`、`inspire` 其他编号或工程路径）
4. **边界与不做的事**（避免范围漂移）

　　涉及**技术选型、架构分层、AI 介入方式、域边界**时，须单独成段写清：

- **考虑过的方案**（至少 2 种，可简写）
- **选定方案的理由**（取舍，不是「因为先进」）
- **代价与风险**（成本、公平、可测性、Phase 门控）
- **验收或「做到什么算够」**

---

## 3 技术名词：可以出现，但不能只出现

| 做法 | 说明 |
| ---- | ---- |
| **首次出现** | 一句通俗解释 +「在商域项目中的具体含义」+ 可选文档/代码落点 |
| **后续使用** | 可直接用简称，但跨节回顾时允许再点一句 |
| **禁止** | 连续多节只有 `Athena`/`RAG`/`LangGraph`/`knobs` 等名词罗列而无解释链 |
| **禁止** | 用「见 79-Wiki」代替正文中的必要解释（Wiki 是索引，不是正文替身） |

　　工程细节（表名、路由、文件路径）**写在表格或附录**，正文优先讲**机制与意图**。

---

## 4 结构与篇幅

### 4.1 推荐章节骨架（构想类长文）

```text
> 文档定位 / 对齐 / 最后更新

## 零、读者与本文价值（可选但推荐）

## 一、问题与目标（Why）

## 二、核心概念与对象（What）

## 三、机制与流程（How）— 配 mermaid 可选

## 四、边界、风险与 Phase 门控（What NOT / When）

## 五、阶段路线与验收

## 六、延伸阅读与文档分工
```

### 4.2 列表与表格的使用

- **列表**：用于步骤、检查项、字段枚举；**不能**替代「为什么」段落。
- **表格**：用于对照、矩阵、现状百分比；表前应有 1 段说明「本表解决什么判断」。
- **单节若超过 8 条 bullet 而无叙述段**：视为文风不合格，须补写引导段或拆节。

### 4.3 与 AI 编程的关系

- 写入 `inspire/` 的**动机、教学意图、产品伦理、叙事**——**不要**为「方便 AI 读」而删成提纲。
- 需要给 AI 用的**硬约束**（Phase、域、禁止项）——应落在 `blueprint-coding.mdc`、`08-`、`DESIGN.md` 或 ADR；`inspire/` 可引用，但**不重复堆叠**同一张禁令表。

---

## 5 文风：提倡与禁止

### 5.1 提倡

- **完整句**：因果清楚，可独立阅读。
- **第一人称「我们」或主理人视角「我」**：构想类文档允许，与「对话体你/你的」区分；投资人向稿件可用第三人称。
- **明确立场**：「我们倾向…」「现阶段不做…因为…」
- **适度篇幅**：说透一个决策，胜过十个词组。

### 5.2 禁止

- **过度简化**：把复杂机制压成一句 slogan 或三个词。
- **刻意精简分析**：在「技术阐述 / 方案对比 / 风险」阶段只留结论不留推理。
- **短促口号式小标题堆砌**：如连续使用「剧情在走」「零 Token」「可成长」而无展开。
- **把「降技术细节」误解为「删掉技术」**：降的是**实现碎屑**（具体库名、算法参数），保留**机制层**说明。

---

## 6 好 / 坏 对照（片段）

### 坏（仅名词 + 清单）

> NPC 用 Role IP + Policy Library + Knobs + Adaptation。对话用状态机。LLM 后置。见 80。

### 好（意图 + 机制 + 边界）

> 我们希望在生涯与赛事中共用同一套角色 IP，让学生感到「同一个陪练人格」贯穿家园与练习局。对局内决策仍由规则策略完成；「成长」体现在局间对策略预设与旋钮的调整，且调整必须可审计、可回滚。对话侧采用状态机约束话题范围，LLM 仅用于措辞润色，并在预算耗尽时降级为模板。

---

## 7 自检清单（完稿前）

- [ ] 每个 `##` 一级节是否有 **≥1 段**「为什么」叙述？
- [ ] 是否存在 **连续两节** 仅有 bullet、无叙述段？若有，是否应合并或补段？
- [ ] 关键决策是否写了 **备选方案 + 取舍**？
- [ ] 名词首次出现是否有一句 **项目内含义**？
- [ ] 是否与 `00～09` / `81-` 等权威文档**矛盾**？（构想可超前，但须标注 Phase）

---

## 8 链接纪律（与 doc-linking 一致）

- **`inspire/` 文内禁止**指向其他 `inspire/**/*.md` 的 Markdown 链接；用 **编号 + 标题** 指称（如 `81-商域 AI 赋能六支柱全景`）。
- **允许向上链**：根目录 `00～09`、`agent.md`、`webapp/**/DESIGN.md`、`docs/decisions/`。
- 读者查专题：文内写「见 inspire/50- 目录」或「见 00- §1.4 索引」，**不要**自建横向外链网。

---

## 9 维护

- 修订 `inspire/` 长文时，默认遵守本规则与 [doc-linking.mdc](./doc-linking.mdc)。
- 若用户明确要求「极简提纲版」，须在文首 `> **文档定位**` 中标注 **「提纲版，非定稿叙述」**。
- 新增 50+ 稳定专题：只改 [inspire/50-](../inspire/50-目录导航.md) 与 [00- §1.4](../00-项目全景与目录结构.md#14-inspire-专题索引权威外链维护点)。
