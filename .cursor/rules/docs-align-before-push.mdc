---
description: 大型更新后、推送 GitHub 前必须先完成根目录说明文档与代码的对齐
alwaysApply: true
---

# 推送前文档对齐（强制）

本仓库以 **根目录 `00～04` + `11-` + `README.md`** 为产品与工程权威；可读性与开发稳定性依赖文档约束。**禁止**在大型更新后、用户要求推送到 GitHub 时，跳过文档对齐直接 `git push`。

## 何时触发

满足任一即视为需执行本规则：

- 用户明确要求推送到 GitHub / 远程
- 本次会话含 **架构、域分包、API 路由、目录迁移、赛制引擎、双端拆分** 等级变更
- 单次提交预计 **≥5 个文件** 或触及 `webapp/backend/app/domains/`、`games/`、`content/`、根目录 `*.md`

小修（ typo、单文件 bugfix、无行为变更）可只做最小文档核对，不必全表刷新。

## 推送前必须完成的步骤（顺序）

1. **盘点代码变更**：`git status` + `git diff --stat`，列出影响模块与对外契约（API、目录、表结构）。
2. **对齐根目录文档**（按变更范围择要更新，不得与代码矛盾）：
   - 必查：`README.md`、`00-PROJECT.md`
   - 产品/赛制：`01-PRODUCT.md`、`02-ARCHITECTURE.md`
   - 术语：`00-TERMINOLOGY.md`（新增 `match_kind`、对外称谓时）
   - 技术摘要：`02-ARCHITECTURE.md`
   - 排期与 P0 进展：`04-ROADMAP.md`（含已落地 / 待办勾选）
   - 工程详表：`03-ENGINEERING.md`（**[AI_DEFAULT](./03-ENGINEERING.md#ai_default)** 快照、§一 API 全表、§五 引擎规范；路由变更时跑 `webapp/scripts/export_openapi.py`）
   - 引擎全栈：`docs/engine-spec.md`（触及运行时/美术管线时）
   - 按需：`inspire/50-` 索引、`inspire/OPC/` 仅当 OPC 规格受影响
3. **统一元数据**：相关文档文首 `最后更新` 改为当日（`YYYY-MM-DD`）；路径以仓库**当前真身**为准（如 `webapp/backend/content/game-configs/`、`inspire/a商赛主题与真题库/`）。
4. **自检**：`03-ENGINEERING.md` [AI_DEFAULT](./03-ENGINEERING.md#ai_default) 与 §一 API 全表、§四 引擎矩阵**无矛盾**；API 与 `webapp/contracts/openapi/bundled.yaml` 或 `main.py` 路由一致；无「待建」项已落地仍标 🔴；`00-PROJECT` §产品架构共识与 `01-`/`02-` 无冲突。
5. **再提交、再推送**：文档对齐与代码变更**同一推送批次**内完成（可先 commit 代码再 commit 文档，或一次 commit，但**不得**只推代码留文档陈旧）。

## 禁止事项

- 在未更新 `03-ENGINEERING.md` 或未在 `04-ROADMAP.md` 反映进展时，声称「已与文档同步」
- 用口头摘要代替对权威 `.md` 文件的实质修改
- 推送后才发现目录/API 与 `00-PROJECT`/`03-ENGINEERING` 不一致

## 完成定义（向用户汇报时）

简要说明已更新的文档编号、以及仍待下一迭代的 🔴 项；然后再执行 `git push`。
