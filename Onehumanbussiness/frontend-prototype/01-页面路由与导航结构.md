# OHB 前端页面路由与导航结构设计

> **文档定位**：定义 OneHumanBusiness 模块的完整 URL 路由体系、权限控制策略、面包屑导航规则以及页面间跳转关系。确保与现有 MVP 路由（`/` 根域下的商赛/课程/生涯系统）无缝融合。
>
> **关联文档**：`../02-系统架构与AI员工体系.md`、`../03-创业孵化流水线.md`、`../../web应用商业教育/frontend/src/App.tsx`
>
> **最后更新**：2026-05-17

---

## 一、路由体系总体设计

### 1.1 路由命名空间隔离

OHB 作为现有平台的「生涯终极阶段」模块，采用 `/ohb/*` 的独立命名空间，避免与现有路由冲突：

```
现有平台路由              OHB 模块路由
─────────────────────────────────────────────
/                         /ohb
/career                   /ohb/career（OHB生涯概览）
/courses                  /ohb/courses（创业课程）
/games                    /ohb/challenges（创业挑战赛）
/wiki                     /ohb/wiki（创业知识库）
                          /ohb/agency（AI员工局）
                          /ohb/tasks（任务管理）
                          /ohb/pipeline（创业流水线）
                          /ohb/bmc（商业模式画布）
                          /ohb/finance（财务看板）
                          /ohb/company/settings（公司设置）
```

### 1.2 路由配置表

| 路由路径 | 页面组件 | 权限等级 | 布局类型 | 面包屑 |
|----------|----------|----------|----------|--------|
| `/ohb` | `OhbLandingPage` | 需要登录 | OHB专属布局 | OHB首页 |
| `/ohb/career` | `OhbCareerPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 生涯概览 |
| `/ohb/agency` | `AgencyPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / AI员工局 |
| `/ohb/agency/hire` | `HireEmployeePage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / AI员工局 / 人才市场 |
| `/ohb/tasks` | `TaskBoardPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 任务管理 |
| `/ohb/tasks/:taskId` | `TaskDetailPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 任务管理 / 任务详情 |
| `/ohb/pipeline` | `PipelineDashboardPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 创业流水线 |
| `/ohb/pipeline/sprint/:sprintId` | `SprintDetailPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 创业流水线 / Sprint详情 |
| `/ohb/bmc` | `BmcEditorPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / BMC编辑器 |
| `/ohb/bmc/history/:versionId` | `BmcHistoryPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / BMC编辑器 / 版本历史 |
| `/ohb/finance` | `FinanceDashboardPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 财务看板 |
| `/ohb/finance/ledger` | `LedgerPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 财务看板 / 记账明细 |
| `/ohb/company/settings` | `CompanySettingsPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 公司设置 |
| `/ohb/integrations` | `IntegrationsPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 外部集成 |
| `/ohb/challenges` | `ChallengesPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 创业挑战赛 |
| `/ohb/reports` | `ReportsPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 报告中心 |
| `/ohb/wiki` | `OhbWikiPage` | 需要登录+OHB解锁 | OHB专属布局 | OHB首页 / 创业百科 |

### 1.3 React Router 配置代码

```typescript
// src/App.tsx 中新增 OHB 路由段
import { lazy, Suspense } from 'react';
import OhbLayout from './components/ohb/OhbLayout';
import OhbGuard from './components/ohb/OhbGuard';

const OhbLandingPage = lazy(() => import('./pages/ohb/OhbLandingPage'));
const AgencyPage = lazy(() => import('./pages/ohb/agency/AgencyPage'));
const TaskBoardPage = lazy(() => import('./pages/ohb/tasks/TaskBoardPage'));
const PipelineDashboardPage = lazy(() => import('./pages/ohb/pipeline/PipelineDashboardPage'));
const BmcEditorPage = lazy(() => import('./pages/ohb/bmc/BmcEditorPage'));
const FinanceDashboardPage = lazy(() => import('./pages/ohb/finance/FinanceDashboardPage'));
const CompanySettingsPage = lazy(() => import('./pages/ohb/company/CompanySettingsPage'));

// 在现有 Routes 内部、Layout Route 中追加：
<Route path="ohb" element={<OhbLayout />}>
  <Route index element={
    <AuthGuard requireAuth>
      <Suspense fallback={<OhbPageSkeleton />}>
        <OhbLandingPage />
      </Suspense>
    </AuthGuard>
  } />
  <Route path="agency" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <AgencyPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="agency/hire" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <HireEmployeePage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="tasks" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <TaskBoardPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="tasks/:taskId" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <TaskDetailPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="pipeline" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <PipelineDashboardPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="pipeline/sprint/:sprintId" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <SprintDetailPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="bmc" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <BmcEditorPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="bmc/history/:versionId" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <BmcHistoryPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="finance" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <FinanceDashboardPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="finance/ledger" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <LedgerPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="company/settings" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <CompanySettingsPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="integrations" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <IntegrationsPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="challenges" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <ChallengesPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="reports" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <ReportsPage />
      </Suspense>
    </OhbGuard>
  } />
  <Route path="wiki" element={
    <OhbGuard>
      <Suspense fallback={<OhbPageSkeleton />}>
        <OhbWikiPage />
      </Suspense>
    </OhbGuard>
  } />
</Route>
```

---

## 二、权限控制设计

### 2.1 三级权限模型

```
┌─────────────────────────────────────────────────────────────┐
│                    权限层级金字塔                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    ┌───────────────┐                        │
│                    │   公开页面     │                        │
│                    │   /ohb/about   │                        │
│                    └───────┬───────┘                        │
│                            │                                │
│              ┌─────────────┴─────────────┐                  │
│              │       需要登录 (Auth)       │                  │
│              │  /ohb (landing预览页)       │                  │
│              └─────────────┬─────────────┘                  │
│                            │                                │
│        ┌───────────────────┴───────────────────┐            │
│        │         需要OHB解锁 (OhbGuard)          │            │
│        │  /ohb/agency, /ohb/tasks, /ohb/pipeline │            │
│        │  /ohb/bmc, /ohb/finance, /ohb/company   │            │
│        └─────────────────────────────────────────┘            │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 OhbGuard 组件设计

```typescript
// src/components/ohb/OhbGuard.tsx
import { Navigate, useLocation } from 'react-router-dom';
import { useAuthStore } from '../../stores/authStore';
import { useOhbStore } from '../../stores/ohbStore';
import { Loader2 } from 'lucide-react';

interface OhbGuardProps {
  children: React.ReactNode;
}

export default function OhbGuard({ children }: OhbGuardProps) {
  const { isAuthenticated, isInitialized } = useAuthStore();
  const { isOhbUnlocked, isOhbLoading } = useOhbStore();
  const location = useLocation();

  // 等待所有状态初始化
  if (!isInitialized || isOhbLoading) {
    return (
      <div className="min-h-screen flex items-center justify-center bg-background">
        <div className="flex flex-col items-center gap-3">
          <Loader2 className="w-8 h-8 text-primary animate-spin" />
          <p className="text-sm text-foreground-muted">加载OHB工作空间...</p>
        </div>
      </div>
    );
  }

  // 未登录 → 跳转登录页，记录返回路径
  if (!isAuthenticated) {
    return <Navigate to="/login" replace state={{ from: location.pathname }} />;
  }

  // 已登录但未解锁OHB → 跳转OHB介绍/解锁页
  if (!isOhbUnlocked) {
    return <Navigate to="/ohb" replace />;
  }

  return <>{children}</>;
}
```

### 2.3 OHB 解锁条件校验

OHB 解锁由后端 `CareerProfile` 中的 `ohb_unlocked` 字段控制，前端通过 `useOhbStore` 在初始化时拉取：

```typescript
// src/stores/ohbStore.ts（权限相关部分）
interface OhbState {
  isOhbUnlocked: boolean;
  isOhbLoading: boolean;
  company: Company | null;
  currentStage: PipelineStage | null;

  // 权限Actions
  checkOhbAccess: () => Promise<void>;
  unlockOhb: () => Promise<void>;
}

export const useOhbStore = create<OhbState>((set, get) => ({
  isOhbUnlocked: false,
  isOhbLoading: true,
  company: null,
  currentStage: null,

  checkOhbAccess: async () => {
    try {
      const res = await api.get('/api/v1/ohb/status');
      set({
        isOhbUnlocked: res.data.data.unlocked,
        company: res.data.data.company,
        currentStage: res.data.data.current_stage,
        isOhbLoading: false,
      });
    } catch {
      set({ isOhbLoading: false, isOhbUnlocked: false });
    }
  },

  unlockOhb: async () => {
    const res = await api.post('/api/v1/ohb/unlock');
    set({ isOhbUnlocked: true, company: res.data.data.company });
  },
}));
```

---

## 三、面包屑导航设计

### 3.1 面包屑数据结构

```typescript
// src/types/ohb.ts
export interface BreadcrumbItem {
  label: string;
  path?: string;        // 无path表示当前页（不可点击）
  icon?: LucideIcon;
}

// 各页面面包屑配置映射
export const OHB_BREADCRUMBS: Record<string, BreadcrumbItem[]> = {
  '/ohb': [{ label: 'OHB首页', icon: Rocket }],
  '/ohb/agency': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: 'AI员工局', icon: Users },
  ],
  '/ohb/agency/hire': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: 'AI员工局', path: '/ohb/agency', icon: Users },
    { label: '人才市场', icon: UserPlus },
  ],
  '/ohb/tasks': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: '任务管理', icon: ClipboardList },
  ],
  '/ohb/pipeline': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: '创业流水线', icon: GitBranch },
  ],
  '/ohb/bmc': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: 'BMC编辑器', icon: LayoutGrid },
  ],
  '/ohb/finance': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: '财务看板', icon: TrendingUp },
  ],
  '/ohb/company/settings': [
    { label: 'OHB首页', path: '/ohb', icon: Rocket },
    { label: '公司设置', icon: Building2 },
  ],
};
```

### 3.2 面包屑组件实现

```typescript
// src/components/ohb/BreadcrumbNav.tsx
import { Link, useLocation } from 'react-router-dom';
import { ChevronRight, Home } from 'lucide-react';
import { OHB_BREADCRUMBS } from '../../types/ohb';

export default function BreadcrumbNav() {
  const location = useLocation();
  const items = OHB_BREADCRUMBS[location.pathname] || [
    { label: 'OHB首页', path: '/ohb', icon: Home },
  ];

  return (
    <nav className="flex items-center gap-2 text-sm text-foreground-muted mb-6">
      {items.map((item, index) => {
        const Icon = item.icon || Home;
        const isLast = index === items.length - 1;
        return (
          <div key={item.label} className="flex items-center gap-2">
            {index > 0 && <ChevronRight className="w-4 h-4 text-foreground-muted/50" />}
            {isLast || !item.path ? (
              <span className="flex items-center gap-1.5 text-foreground font-medium">
                <Icon className="w-4 h-4" />
                {item.label}
              </span>
            ) : (
              <Link
                to={item.path}
                className="flex items-center gap-1.5 hover:text-primary transition-colors"
              >
                <Icon className="w-4 h-4" />
                {item.label}
              </Link>
            )}
          </div>
        );
      })}
    </nav>
  );
}
```

---

## 四、OHB 专属布局（OhbLayout）

### 4.1 布局结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  OHB Layout                                                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────┐  ┌───────────────────────────────────────────────────────┐   │
│  │          │  │  BreadcrumbNav                                        │   │
│  │  OHB     │  ├───────────────────────────────────────────────────────┤   │
│  │ Sidebar  │  │                                                       │   │
│  │          │  │  <Outlet /> — 各页面内容                               │   │
│  │ (固定)   │  │                                                       │   │
│  │          │  │                                                       │   │
│  └──────────┘  └───────────────────────────────────────────────────────┘   │
│       ↑                      ↑                                              │
│   w-64固定               flex-1 自适应                                       │
│   lg:fixed h-full                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 OHB Sidebar 导航项

```typescript
// src/components/ohb/OhbSidebar.tsx
const ohbNavItems = [
  { path: '/ohb', label: '控制台', icon: LayoutDashboard },
  { path: '/ohb/agency', label: 'AI员工局', icon: Users, badge: 'activeEmployees' },
  { path: '/ohb/tasks', label: '任务管理', icon: ClipboardList, badge: 'pendingTasks' },
  { path: '/ohb/pipeline', label: '创业流水线', icon: GitBranch, highlight: true },
  { path: '/ohb/bmc', label: 'BMC画布', icon: LayoutGrid },
  { path: '/ohb/finance', label: '财务看板', icon: TrendingUp },
  { path: '/ohb/challenges', label: '挑战赛', icon: Trophy },
  { path: '/ohb/reports', label: '报告中心', icon: FileText },
  { path: '/ohb/wiki', label: '创业百科', icon: BookOpen },
];

const ohbSettingItems = [
  { path: '/ohb/company/settings', label: '公司设置', icon: Building2 },
  { path: '/ohb/integrations', label: '外部集成', icon: Plug },
];
```

---

## 五、页面间跳转关系图

### 5.1 全局跳转关系

```
                              ┌─────────────┐
                              │   /ohb      │
                              │  OHB控制台   │
                              └──────┬──────┘
                                     │
        ┌────────────┬───────────────┼───────────────┬────────────┐
        ▼            ▼               ▼               ▼            ▼
   ┌─────────┐  ┌─────────┐   ┌───────────┐   ┌─────────┐  ┌──────────┐
   │ /agency │  │ /tasks  │   │ /pipeline │   │ /bmc    │  │ /finance │
   │ AI员工局 │  │任务管理  │   │ 创业流水线 │   │BMC画布  │  │ 财务看板  │
   └────┬────┘  └────┬────┘   └─────┬─────┘   └────┬────┘  └────┬─────┘
        │            │              │              │            │
        ▼            ▼              ▼              ▼            ▼
   ┌─────────┐  ┌─────────┐   ┌───────────┐   ┌─────────┐  ┌──────────┐
   │/hire    │  │/:taskId  │   │/sprint/:id│   │/history │  │ /ledger  │
   │人才市场  │  │任务详情  │   │ Sprint详情 │   │版本历史  │  │ 记账明细  │
   └─────────┘  └─────────┘   └───────────┘   └─────────┘  └──────────┘
        │            │              │              │            │
        └────────────┴──────────────┴──────────────┴────────────┘
                                     │
                              ┌──────┴──────┐
                              │/company/     │
                              │  settings    │
                              │  公司设置     │
                              └─────────────┘
```

### 5.2 关键跳转逻辑

| 起始页面 | 跳转目标 | 触发条件 | 跳转方式 |
|----------|----------|----------|----------|
| `/ohb` (未解锁) | `/career` | 点击「了解 prerequisite」 | `<Navigate>` |
| `/ohb` (已解锁无公司) | `/ohb/company/settings` | 首次进入，自动引导创建公司 | `useEffect` 自动跳转 |
| `/ohb/agency` | `/ohb/agency/hire` | 点击「招聘新员工」按钮 | `<Link>` |
| `/ohb/agency` | `/ohb/tasks` | 点击员工「查看任务」 | `<Link>` |
| `/ohb/tasks` | `/ohb/tasks/:taskId` | 点击任务卡片 | `<Link>` |
| `/ohb/tasks/:taskId` | `/ohb/agency` | 点击执行员工头像 | `<Link>` |
| `/ohb/pipeline` | `/ohb/bmc` | IDEATE阶段点击「编辑BMC」 | `<Link>` |
| `/ohb/pipeline` | `/ohb/tasks` | 点击当前Sprint任务列表 | `<Link>` |
| `/ohb/pipeline` | `/ohb/pipeline/sprint/:sprintId` | 点击历史Sprint | `<Link>` |
| `/ohb/bmc` | `/ohb/bmc/history/:versionId` | 点击「版本历史」入口 | `<Link>` |
| `/ohb/finance` | `/ohb/finance/ledger` | 点击「查看明细」 | `<Link>` |
| 任意页面 | `/ohb/company/settings` | 点击顶部公司Logo下拉菜单 | `<Link>` |
| 任意页面 | `/ohb/integrations` | 设置菜单中点击「外部集成」 | `<Link>` |

### 5.3 带状态保留的跳转

任务详情页（`/ohb/tasks/:taskId`）返回任务看板时保留筛选状态：

```typescript
// TaskBoardPage → TaskDetailPage 跳转时
navigate(`/ohb/tasks/${taskId}`, {
  state: { fromBoard: true, filters: currentFilters }
});

// TaskDetailPage 返回时
const location = useLocation();
const navigateBack = () => {
  if (location.state?.fromBoard) {
    navigate('/ohb/tasks', { state: { preserveFilters: location.state.filters } });
  } else {
    navigate('/ohb/tasks');
  }
};
```

---

## 六、导航状态指示

### 6.1 实时状态徽章

Sidebar 导航项支持动态徽章，由 Zustand store 驱动：

```typescript
// 在 OhbSidebar 中
const { activeEmployees, pendingTasks, currentStage } = useOhbStore();

const badgeMap: Record<string, number | string> = {
  activeEmployees: activeEmployees,
  pendingTasks: pendingTasks,
};
```

### 6.2 阶段高亮

当前所处流水线阶段在 Sidebar 中以脉冲动画高亮：

```css
/* index.css 中追加 */
.pipeline-active-glow {
  animation: pipelinePulse 2s ease-in-out infinite;
}

@keyframes pipelinePulse {
  0%, 100% { box-shadow: 0 0 0 0 rgba(212, 168, 83, 0.2); }
  50% { box-shadow: 0 0 0 4px rgba(212, 168, 83, 0); }
}
```

---

*下一篇：[02-关键页面原型设计.md](./02-关键页面原型设计.md)*
